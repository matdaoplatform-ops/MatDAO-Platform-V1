# v0 runtime files (should only exist in preview, not in git)
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts

# Common ignores
node_modules/
.next/
.env*.local
.DS_Store

# Hardhat build output (frontend ABIs are committed under lib/web3/abi instead)
artifacts/
cache/
typechain-types/
# ephemeral local deployments (deployments/sepolia.json is committed)
deployments/hardhat.json
deployments/localhost.json